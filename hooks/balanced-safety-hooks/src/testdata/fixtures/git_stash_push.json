{
  "name": "git_stash_push",
  "description": "git stash push -u -m \"...\" creates a new stash — ask first",
  "input": {
    "tool_name": "Bash",
    "tool_input": {
      "command": "git stash push -u -m \"wip\""
    },
    "cwd": "/home/example-user/myproject"
  },
  "expect": {
    "decision": "ask",
    "rule": "git",
    "reason_code": "git.stash_create"
  }
}
